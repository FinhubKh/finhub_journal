# Copy to .env and fill in from Supabase Dashboard → Settings → API

VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# IMPORTANT: use "anon" / "public" key — NOT service_role
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only — never put this in the EA or frontend
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# EA sync API (your own endpoint — not Supabase)
# Local dev: leave unset and run npm run api:dev (defaults to http://localhost:8787/v1/ea/sync)
# Production: https://api.yourdomain.com/v1/ea/sync
# EA_API_URL=https://api.yourdomain.com/v1/ea/sync
# EA_API_PORT=8787

# After updating .env:
#   npm run db:check     — verify tables exist
#   npm run api:dev      — start EA sync API (local)
#   npm run ea:config    — regenerate MT5 EA config

# ── MetaAPI (disabled — paid cloud sync) ─────────────────────
# VITE_METAAPI_ENABLED=true
# METAAPI_TOKEN=your-metaapi-token
