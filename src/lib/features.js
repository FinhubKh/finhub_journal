/**
 * Feature flags — loaded from root .env via Vite.
 * MetaAPI is disabled for now (paid service). Re-enable in features.js when ready.
 */

// export const metaApiEnabled = import.meta.env.VITE_METAAPI_ENABLED === 'true';
export const metaApiEnabled = false;
